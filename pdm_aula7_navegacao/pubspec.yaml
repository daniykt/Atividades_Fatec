name: pdm_aula7_navegacao
description: App de checkout com estado local (setState) e navegação entre telas (Navigator) — PDM I, Aula 7.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
